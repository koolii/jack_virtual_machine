### 実装
VMの実装は大きく「VMの世界をエミュレートする」、「VMコマンドを一連の命令に変換する」ことの2つとなる

標準マッピング＝「VM要素からHackハードウェアへのマッピング」と「VM命令からと機械語へのマッピング」の事を言う
今回作るソフトウェアは「VM implementation」または「VM translator」と呼ぶ

以下、VMの標準マッピングを示す

※標準マッピングを示す事で以下の事を保証することができる
「VMベースのプログラムがこのVMを使用しないコンパイラによっっっって生成されるプログラムと相互にやり取りする方法についての使用が、ガイドラインとして含まれている」
「別の人がテストやソフトウェアを掛けるようにVMを実装する開発者に標準テストを行わせることができる」

# 標準マッピング

## VMからHackへの変換
1つ以上からなる`.vm`ファイルの集合であり、それら全てを1つの入力として渡し、最終的にHackアセンブリ言語で書かれた`.asm`ファイルを1つだけ生成する
(`.vm`ファイルに含まれる関数の順番はどのような順番であっても問題にならない)

## RAMの使用法(スクショにRAMアドレス毎の役割を記述しているものを撮影)
Hackコンピュータのデータメモリは32K個の16ビットワード
最初の16K個のデータメモリは汎用的なRAMで、その後の16KコはI/Oデバイスのメモリマップを含む

RAMアドレスの0から15番目はR0からR15のシンボルを使うことが出来るがこれはVM実装の可読性を高める目的を見通してアセンブリ言語に導入したもの

## メモリセグメントマッピング